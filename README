Building
========

I made some changes to the original BYACC/J Makefile to allow it to compile on
Linux. You may need to customize it to your system. 

Try:

$ cd src
$ make linux

Which should produce an executable inside the src/ directory called `yacc.linux'.

